apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nam-template.fullname" . }}-test-connection"
  labels:
    {{- include "nam-template.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nam-template.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
